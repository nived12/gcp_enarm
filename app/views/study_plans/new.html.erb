<% content_for :title, t("study_plans.new.title") %>

<section>
  <p class="eyebrow"><%= t("study_plans.eyebrow") %></p>
  <h1 class="mt-2 text-2xl font-semibold tracking-tight text-ink"><%= t("study_plans.new.title") %></h1>
  <p class="mt-3 max-w-prose text-sm leading-relaxed text-ink-muted"><%= t("study_plans.new.intro") %></p>
</section>

<%= render "form", plan: @plan, error: @error, submit: t("study_plans.new.submit") %>
